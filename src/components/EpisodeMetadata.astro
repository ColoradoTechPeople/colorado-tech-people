---
import { formatPublishDate } from "@lib/episodes";

interface Props {
  publishDate?: string;
}

const { publishDate } = Astro.props;
const formattedDate = formatPublishDate(publishDate);
---

{formattedDate && (
  <p class="episode-card__meta">
    <span>{formattedDate}</span>
  </p>
)}
